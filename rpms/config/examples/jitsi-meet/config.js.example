// /etc/jitsi/meet/meet.example.com-config.js
// Замените meet.example.com на ваш домен.

var config = {
    hosts: {
        domain: 'meet.example.com',
        anonymousdomain: 'guest.meet.example.com',
        muc: 'conference.meet.example.com',
        focus: 'focus.meet.example.com',
    },
    bosh: 'https://meet.example.com/http-bind',
    websocket: 'wss://meet.example.com/xmpp-websocket',
    enableWelcomePage: true,
    enableUserRolesBasedOnToken: false,
};

// Keycloak SSO (раскомментируйте после настройки Keycloak):
// config.tokenAuthUrl = 'https://meet.example.com/realms/jitsi-realm/protocol/openid-connect/auth?client_id=jitsi&response_type=code&scope=openid&state={state}&redirect_uri=https://meet.example.com/static/sso.html&code_challenge={code_challenge}&code_challenge_method=S256';
// config.tokenLogoutUrl = 'https://meet.example.com/realms/jitsi-realm/protocol/openid-connect/logout?post_logout_redirect_uri=https://meet.example.com/static/logout.html';
// config.tokenAuthInline = true;

// File sharing (после установки jitsi-meet-file-sharing-service):
// config.fileSharing = {
//     enabled: true,
//     apiUrl: 'https://meet.example.com/file-service/v1/documents',
// };
